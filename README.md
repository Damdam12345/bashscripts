<h4>Backup2.sh- simple backupscript that attach date to the time of the backup.</h4>
<h4>Install.sh- using if statements to check if java is present and the latest version is installed.</h4>
<h4>Process.sh- using case statements to sort user process by memory and cpu.</h4>
